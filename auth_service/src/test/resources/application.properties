# ==========================================================
# CONFIGURACIÓN DE TEST (TESTCONTAINERS SE ENCARGA DE LA BD)
# ==========================================================
spring.profiles.active=test

# Testcontainers inyectará spring.datasource.url automáticamente.
# Hibernate settings básicos:
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# ==========================================================
# VALORES DUMMY (MOCKS) OBLIGATORIOS
# ==========================================================
# Aunque la BD sea real (Docker), necesitamos estas claves para
# que los @Value("${...}") de tu código no exploten.

JWT_SECRET=clave_super_secreta_para_tests_unitarios_123456789012345
RESEND_API_KEY=re_123_test_key
TWILIO_SID=AC_test_sid
TWILIO_TOKEN=test_token

# Estilo Properties (Puntos)
resend.api.key=re_123_test_key_dummy
twilio.account.sid=AC_test_sid_dummy
twilio.auth.token=test_token_dummy
twilio.phone.number=+1234567890

# Variables placeholder (Testcontainers las sobrescribirá, pero por si acaso)
DB_URL=jdbc:postgresql://localhost:5432/testdb
DB_USER=test
DB_PASSWORD=test