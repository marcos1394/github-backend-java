name: QuHealthy Enterprise Pipeline 🏥🚀

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize, reopened]

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  REPO_NAME: quhealthy-repo

jobs:
  # ========================================================================
  # JOB 1: CALIDAD Y SEGURIDAD (SonarCloud - Multi-Module)
  # ========================================================================
  sonar-analysis:
    name: Code Quality & Security
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 👇 AQUI ESTÁN TODOS TUS SERVICIOS (AHORA INCLUYE NOTIFICATION)
        service: [auth_service, onboarding_service, social_service, payment_service, review_service, catalog_service, appointment_service, notification_service]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # Analizamos cada servicio por separado según la matriz
      - name: Build and Analyze ${{ matrix.service }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          # Sonar a veces requiere compilar, por lo que necesita un JWT dummy si los tests corren
          JWT_SECRET: ${{ secrets.JWT_SECRET }} 
        # Navegamos a la carpeta del servicio antes de ejecutar Maven
        run: |
          cd ${{ matrix.service }}
          mvn -B verify -e -Dsurefire.useFile=false \
          -DskipTests=true \
          org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
          -Dsonar.projectKey=marcos1394_github-backend-java \
          -Dsonar.organization=marcos1394 \
          -Dsonar.host.url=https://sonarcloud.io

  # ========================================================================
  # JOB 2: BUILD & DEPLOY (Matrix Strategy)
  # ========================================================================
  deploy:
    needs: sonar-analysis
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    strategy:
      fail-fast: false # Si falla uno, que los otros sigan intentando
      matrix:
        include:
          # --- CONFIGURACIÓN AUTH SERVICE ---
          - service-name: auth-service
            directory: auth_service
            # Secretos: Auth necesita TODOS (Stripe, Twilio, Resend, DB, JWT)
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest
              STRIPE_API_KEY=application_stripe_key:latest
              RESEND_API_KEY=application_resend_key:latest
              TWILIO_SID=application_twilio_sid:latest
              TWILIO_TOKEN=application_twilio_token:latest
              TWILIO_PHONE_NUMBER=application_twilio_phone:latest
              RESEND_FROM_EMAIL=RESEND_FROM_EMAIL:latest
              GOOGLE_CLIENT_ID=application_google_client_id:latest
              GOOGLE_CLIENT_SECRET=application_google_client_secret:latest

          # --- CONFIGURACIÓN ONBOARDING SERVICE ---
          - service-name: onboarding-service
            directory: onboarding_service
            # Secretos: Onboarding es ligero, SOLO necesita DB y JWT
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest

          # --- CONFIGURACIÓN SOCIAL SERVICE ---
          - service-name: social-service
            directory: social_service
            # Secretos: DB, JWT y las llaves de Facebook/Meta/GoogleAI
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest
              FACEBOOK_APP_ID=application_facebook_app_id:latest
              FACEBOOK_APP_SECRET=application_facebook_app_secret:latest
              FACEBOOK_REDIRECT_URI=application_facebook_redirect_uri:latest
              GOOGLE_AI_API_KEY=google-ai-api-key:latest

          # --- CONFIGURACIÓN PAYMENT SERVICE ---
          - service-name: payment-service
            directory: payment_service
            # Secretos: DB, JWT, Stripe y MercadoPago
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest
              STRIPE_API_KEY=application_stripe_key:latest
              STRIPE_WEBHOOK_SECRET=application_stripe_webhook_secret:latest
              MP_ACCESS_TOKEN=application_mp_access_token:latest
              MP_WEBHOOK_SECRET=application_mp_webhook_secret:latest

          # --- CONFIGURACIÓN REVIEW SERVICE ---
          - service-name: review-service
            directory: review_service
            # Secretos: DB y JWT (Google Pub/Sub usa la identidad del servicio)
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest

          # --- CONFIGURACIÓN CATALOG SERVICE ---
          - service-name: catalog-service
            directory: catalog_service
            # Secretos: DB y JWT (Lectura/Escritura de servicios)
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest
              GOOGLE_MAPS_API_KEY=application_google_maps_key:latest


          # --- CONFIGURACIÓN APPOINTMENT SERVICE 📅 ---
          - service-name: appointment-service
            directory: appointment_service
            # Secretos: DB y JWT
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest

          # --- CONFIGURACIÓN NOTIFICATION SERVICE (NUEVO 🔔) ---
          - service-name: notification-service
            directory: notification_service
            # Secretos: DB, JWT y CANALES (Resend + Twilio)
            secrets-list: |
              DB_URL=application_db_url:latest
              DB_USER=application_db_user:latest
              DB_PASSWORD=application_db_password:latest
              JWT_SECRET=application_jwt_secret:latest
              RESEND_API_KEY=application_resend_key:latest
              RESEND_FROM_EMAIL=RESEND_FROM_EMAIL:latest
              TWILIO_SID=application_twilio_sid:latest
              TWILIO_TOKEN=application_twilio_token:latest
              TWILIO_PHONE_NUMBER=application_twilio_phone:latest

    steps:
      - uses: actions/checkout@v4

      # 1. Autenticación Google Cloud (Workload Identity Federation)
      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v2'
        with:
          workload_identity_provider: '${{ secrets.GCP_WIF_PROVIDER }}'
          service_account: '${{ secrets.GCP_SA_EMAIL }}'

      # 2. Setup SDK
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      # 3. Docker Auth
      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      # 4. Build & Push (Dinámico según la matriz)
      - name: Build and Push ${{ matrix.service-name }}
        run: |
          IMAGE_URI=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ matrix.service-name }}:${{ github.sha }}
          
          # Entramos al directorio específico
          cd ${{ matrix.directory }}
          
          # Construimos usando el Dockerfile de esa carpeta
          docker build -t $IMAGE_URI .
          docker push $IMAGE_URI

      # 5. Deploy to Cloud Run (Dinámico)
      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ matrix.service-name }}
          region: ${{ env.REGION }}
          image: ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ matrix.service-name }}:${{ github.sha }}
          
          # Variables de entorno públicas
          env_vars: |
            SPRING_PROFILES_ACTIVE=prod
          
          # Inyección de Secretos desde Google Secret Manager
          secrets: ${{ matrix.secrets-list }}