# ===================================================================
# 🏗️ CONFIGURACIÓN DE SERVIDOR & APP
# ===================================================================
spring.application.name=social-service
# Puerto 8083 por defecto en local para no chocar con Auth (8082) u Onboarding (8081)
# En Cloud Run, la variable PORT=8080 sobreescribirá esto automáticamente.
server.port=${PORT:8083}

# ===================================================================
# 💾 BASE DE DATOS (PostgreSQL)
# ===================================================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA & Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ===================================================================
# 🛡️ SEGURIDAD & JWT
# ===================================================================
# CRÍTICO: Aquí mapeamos la ENV 'JWT_SECRET' a la propiedad que busca tu JwtAuthenticationFilter.java
application.security.jwt.secret-key=${JWT_SECRET}

# ===================================================================
# ☁️ GOOGLE CLOUD PLATFORM (Storage & AI)
# ===================================================================
# El Project ID se suele inyectar, pero es bueno tenerlo explícito o por defecto
spring.cloud.gcp.project-id=${GCP_PROJECT_ID:quhealthy-backend}
spring.cloud.gcp.storage.enabled=true
# Nombre del bucket para subir imágenes (puedes cambiarlo si creas uno específico)
spring.cloud.gcp.storage.bucket-name=quhealthy-media

# ===================================================================
# 📱 REDES SOCIALES (Mapeo de Secretos)
# ===================================================================
# Estas propiedades deben coincidir con los @Value en SocialAuthService.java
facebook.app-id=${FACEBOOK_APP_ID}
facebook.app-secret=${FACEBOOK_APP_SECRET}
facebook.redirect-uri=${FACEBOOK_REDIRECT_URI}

# ===================================================================
# 💓 HEALTH CHECKS (Vital para que Cloud Run sepa que estás vivo)
# ===================================================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}

# ===================================================================
# 🕵️‍♂️ LOGGING
# ===================================================================
logging.level.root=INFO
logging.level.com.quhealthy=DEBUG
# Para ver detalles de seguridad si algo falla
logging.level.org.springframework.security=INFO